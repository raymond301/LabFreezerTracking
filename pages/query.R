querypage <- dashboardPage(
  dashboardHeader(disable = TRUE),
  dashboardSidebar(),
  dashboardBody(
    h3("TBD"),
    p("Under Construction"),
    p("...will contain all advanced search & summarization features.")
    )
)